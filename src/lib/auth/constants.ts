/** Kept free of server-only imports so the edge middleware can use it. */
export const SESSION_COOKIE = "matesther_session";

/** Session lifetime in seconds. */
export const SESSION_TTL_SECONDS = 60 * 60 * 8;
